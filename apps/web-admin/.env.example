# ─── Supabase (client-side — publishable key only) ───────────────────────────
# These are safe to expose in the browser bundle.
# Get them from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://yplkmowedhdcclxdgtst.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_NzbjbbxVud_ZZmHcpP0XBw_Pw-vgUIc

# ─── API (only needed if you override the Vite proxy) ────────────────────────
# In development the Vite proxy (vite.config.ts) forwards /api → localhost:3000.
# Set this only if you point the web-admin at a remote API.
# VITE_API_URL=http://localhost:3000
